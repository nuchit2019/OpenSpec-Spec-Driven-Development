#  OpenSpec เครื่องมือจัดการสเปก เพื่อใช้ AI เขียนโค้ดได้อย่างมั่นใจ

OpenSpec คือ **สรุปโครงการ **Fission-AI /**OpenSpec** *(จาก GitHub)*

#

## 📌 ภาพรวมของ **OpenSpec**

**OpenSpec** เป็นโปรเจกต์ **Open Source** จาก Fission AI ที่มุ่งเน้นไปที่ **Spec-Driven Development (SDD)** สำหรับการทำงานร่วมกับ AI Coding Assistants — เช่น Claude Code, Cursor, GitHub Copilot 等
และช่วยให้ทั้งคนและ AI **ตกลงกันบน “spec (ข้อกำหนด)” ก่อนเขียนโค้ดจริง** เพื่อผลลัพธ์ที่แม่นยำและตรวจสอบได้มากขึ้น โดยไม่ต้องใช้ API Key ใด ๆ ([GitHub][1])

#

## 🧠 ปัญหาที่ OpenSpec แก้ไข

โดยปกติเมื่อเราคุยกับ AI เพื่อให้ช่วยเขียนโค้ด:

* ความต้องการมักอยู่ในประวัติการคุย → ทำให้ AI สับสนหรือเขียนผิดเป้าหมาย
* ความไม่ชัดเจนของ requirement → โค้ดที่ได้ไม่ตรงงานจริง

**OpenSpec ใช้ workflow แบบ “Agreement → Implementation”** ก่อนจะให้ AI เขียนโค้ดจริง เพิ่มความชัดเจนและคาดเดาผลลัพธ์ได้ดีขึ้น ([평범한 직장인이 사는 세상][2])

#

## 🛠️ วิธีการทำงาน (Workflow)

หลักการของ OpenSpec คือแบ่งงานเป็น *ขั้นตอน* ที่ชัดเจนมากขึ้น เช่น:

1. **Draft Change / Proposal**
   – สร้างข้อเสนอ spec ใหม่ (why & what)
2. **Review & Align**
   – ตรวจสอบ spec ให้ทั้งคนและ AI เข้าใจตรงกัน
3. **Implement Tasks**
   – ใช้ AI ช่วยทำตาม list ของ task ที่ตกลงไว้
4. **Archive & Update Specs**
   – เมื่องานเสร็จ → รวมการเปลี่ยนแปลงกลับเข้า “source of truth” ของ spec

โครงสร้างไฟล์ในโปรเจกต์จะมีสองส่วนหลัก:

* `openspec/specs/` — truths ปัจจุบันของระบบ
* `openspec/changes/` — ข้อเสนอหรือการเปลี่ยนแปลงที่ยังไม่ได้ approve

ปลายทางคือ spec ที่สามารถตรวจสอบ, ทดสอบ และติดตามได้อย่างเป็นระบบ ([GitHub][3])

#

## 🧩 คุณสมบัติสำคัญ

✔️ **Spec-Driven Development (SDD)** – ระบุข้อกำหนดอย่างเป็นระบบก่อนโค้ด
✔️ **Reviewable & Auditable** – ทุกการเปลี่ยนแปลงตรวจสอบได้
✔️ **Works with Many AI Tools** – รองรับ Slash Cmd หรือ workflow กับ Claude, Cursor, Copilot, Qoder, OpenCode เป็นต้น ([GitHub][3])
✔️ **Lightweight & No API Key Needed** – ใช้กับเครื่องมือที่มีอยู่ได้ทันที
✔️ **Structured Change Management** – แยก Proposal / Tasks / Specs ออกจากกัน ทำให้ tracking ง่าย ([GitHub][1])

#

## 💡 เหมาะกับใคร?

🔹 Teams หรือ developers ที่ต้องการให้ AI ทำงาน
🔹 อยากลดความลำเอียงของ AI จาก prompt แบบ freeform
🔹 ต้องการ workflow ที่ชัดเจนและตรวจสอบได้
🔹 ต้องการนำ SDD ไปใช้ใน CI/CD หรือกระบวนการพัฒนาเต็มรูปแบบ ([Jimmy Song][4])

#

## 📦 ตัวอย่างการใช้งาน

```bash
npm install -g @fission-ai/openspec@latest
cd my-project
openspec init
```

จากนั้นรันคำสั่งอย่าง:

* `openspec list` — ดูการเปลี่ยนแปลงที่ค้างอยู่
* `openspec show <change>` — ดูรายละเอียด proposal
* `openspec validate <change>` — ตรวจสอบ spec
* `openspec archive <change>` — merge spec กลับ ([GitHub][3])

#

## 🧾 สรุป — ข้อดีของ OpenSpec

✨ ทำให้การใช้ AI เขียนโค้ด **มีขั้นตอน มีโครงสร้าง และ predictable**
✨ Specs ถูกจัดเก็บแบบชัดเจนเหมือน source of truth
✨ ติดตามได้ว่า “อะไรเปลี่ยนไป ทำไมต้องเปลี่ยน”
✨ ไม่ต้องพึ่ง prompt ความจำของ AI ล้วน ๆ
✨ รองรับหลาย AI tools และ workflow ([toolify.ai][5])

#

ถ้าคุณต้องการฉบับ **ตัวอย่าง workflow จริง ๆ** หรืออยากให้ผมสรุป **โครงสร้างของไฟล์/โฟลเดอร์ภายใน repo ให้อ่านง่าย ๆ** บอกได้เลยครับ!

[1]: https://github.com/Fission-AI/OpenSpec?utm_source=chatgpt.com "Fission-AI/OpenSpec: Spec-driven development for AI ..."
[2]: https://digitalbourgeois.tistory.com/2155?utm_source=chatgpt.com "AI 코딩의 불확실성을 줄이는 새로운 접근, OpenSpec"
[3]: https://github.com/Fission-AI/OpenSpec "GitHub - Fission-AI/OpenSpec: Spec-driven development (SDD) for AI coding assistants."
[4]: https://jimmysong.io/ai/openspec/?utm_source=chatgpt.com "OpenSpec | Jimmy Song"
[5]: https://www.toolify.ai/ai-news/openspec-streamlining-specdriven-ai-development-simply-3894563?utm_source=chatgpt.com "OpenSpec: Streamlining Spec-Driven AI Development ..."
